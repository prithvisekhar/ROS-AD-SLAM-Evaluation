language:
  - cpp
  - python
python:
  - "2.7"
compiler:
  - gcc
before_install: # Use this to prepare the system to install prerequisites or dependencies
  # Define some config vars
  - export ROS_DISTRO=kinetic
  - export CI_SOURCE_PATH=$(pwd)
  - echo "Testing branch $TRAVIS_BRANCH "
  - pwd
  - sudo sh -c 'echo "deb http://packages.ros.org/ros/ubuntu $(lsb_release -sc) main" > /etc/apt/sources.list.d/ros-latest.list'
  - wget http://packages.ros.org/ros.key -O - | sudo apt-key add -
  - sudo apt-get update
  - sudo apt-get install -y python-catkin-pkg python-rosdep python-wstool ros-$ROS_DISTRO-roscpp
  - sudo rosdep init
  - rosdep update
install: # Use this to install any prerequisites or dependencies necessary to run your build
  # Create workspace
  - cd ~
  - git clone https://github.com/prithvisekhar/ROS-AD-SLAM-Evaluation.git
  - mkdir -p ~/catkin_ws/src
  - cd ~/catkin_ws/src
  - cp -r ~/ROS-AD-SLAM-Evaluation/catkin_bak/src/ros-bridge ~/catkin_ws/src
  - ls
before_script: # Use this to prepare your build for testing e.g. copy database configurations, environment variables, etc.
  - source /opt/ros/$ROS_DISTRO/setup.bash  
script: # All commands must exit with code 0 on success. Anything else is considered failure.
  # TEST 1 -------------------------------------------
  - echo "Test 1 ========================================"
  - catkin_make DCMAKE_BUILD_TYPE=Debug
